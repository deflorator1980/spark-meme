// import scala.util.Random
// val n = Math.pow(10, 8).toInt
// val start = System.nanoTime
// val pi = (1 to n).map(a => if((Math.pow(Random.nextDouble-1, 2) + Math.pow(Random.nextDouble-1, 2))<=1) 1 else 0 ).par.sum * 4.0/ n
// val stop  = System.nanoTime
// println(pi + " Time: " + (stop - start) / Math.pow(10.0, 9.0))
